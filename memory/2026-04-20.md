## 2026-04-20

### 變更目的
- 清理舊版種子流程遺留的膠囊廣場貼文，避免「秘密膠囊不自動貼廣場」已修正後，畫面仍殘留歷史 100 筆資料造成混淆。

### 後端調整
- 檔案：`spacetimedb/src/index.ts`
- 新增 reducer：`dev_cleanup_seed_capsule_square_posts`
  - 僅清理 `sourceKind = capsule` 且寄件者屬於 demo seed 信箱（`isDemoSeedMailbox`）的廣場貼文。
  - 使用既有 `deleteSquarePostCascade(ctx, post.sourceMessageId)`，同步清除該貼文相關的按讚、收藏、留言等關聯資料。

### 已執行
- `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
- `npm run spacetime:publish`
- `spacetime call -y idea-jd2zx dev_cleanup_seed_capsule_square_posts --server maincloud`

### 結果
- 既有「膠囊來源」的 demo seed 廣場貼文已被清理。
- 後續再次執行膠囊 seed 時，不會再自動新增到廣場（維持目前分流規則）。

---

### 介面調整（聊聊記錄）
- 檔案：`src/App.tsx`
- 將「聊聊」從膠囊彈窗內的局部區塊，升級為可持續聊天流程：
  - 新增 `activeTab = 'chat'`。
  - 在「我的」新增「聊聊記錄」卡片，顯示可續聊的聊天線數量。
  - 左欄可瀏覽聊天線（以 `sourceMessageId + threadGuestIdentity` 聚合），顯示對象、主文摘要、最後一句。
  - 右欄新增聊天視窗，顯示歷史訊息並可繼續送出。
  - 膠囊彈窗的「聊聊」按鈕改為直接跳轉到「我的 > 聊聊記錄」對應聊天線。

### 驗證
- `npm run build` 已通過。

---

### 種子暱稱未更新問題修正
- 問題原因：`dev_seed_demo_users` 受 `module_migration_done` 一次性鎖控制，原本 migration id 為 `dev_seed_demo_users_v3`，導致後續名單更新後不會再覆蓋既有種子資料。
- 調整內容：`spacetimedb/src/index.ts`
  - `SEED_DEMO_USERS_MIGRATION` 由 `dev_seed_demo_users_v3` 升級為 `dev_seed_demo_users_v4`。
  - 同步更新註解說明。
- 已執行：
  - `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
  - `npm run spacetime:publish`
  - `npm run spacetime:seed-demo`
- 結果：
  - 100 筆 `@inbox.volta.test` 種子帳號暱稱已成功覆蓋更新。
  - 抽查樣本：`niuheihei17745x2r@...` → `一隻檸檬精`、`malongyan99826x2q@...` → `芝麻醬味少女`。

---

### 300 筆秘密膠囊（僅膠囊、不上廣場）
- 需求：使用現有 100 種子帳號，新增總計 300 筆秘密膠囊，且不得生成廣場貼文。
- 調整：
  - `spacetimedb/src/index.ts`
    - `dev_seed_secret_capsule_posts` 的 `slot` 範圍從 `0–99` 擴至 `0–299`。
    - 發信帳號改為 `slot % 100` 對應帳號。
    - `capsule_message.id` 改用實際 `slot`（`demoCapsuleScheduledMessageId(slot)`），避免 100 筆冪等覆蓋。
    - 內容與時間偏移改用 `slot` 生成，確保 300 筆有獨立內容與時間分布。
  - `spacetimedb/scripts/run-seed-capsule-slots.mjs`
    - loop 從 100 次改為 300 次（`slot 0..299`）。
- 已執行：
  - `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
  - `npm run spacetime:publish`
  - `npm run spacetime:seed-capsules`
- 驗證：
  - `capsule_message` = **300**
  - `square_post` = **0**

---

### 前端修正：膠囊抽取資料源
- 問題：秘密頁面的「抽一則秘密膠囊」仍綁定 `square_post`，當需求改為「膠囊不上廣場」時，抽取區會顯示 0 筆。
- 調整檔案：`src/App.tsx`
  - 抽取池改為讀取 `tables.capsuleMessage`（已到開啟時間且排除自己）。
  - 抽取 id、換一則、去重 key 全改為 `capsule_message.id`。
  - 彈窗內容改顯示 `capsule_message.content/scheduledAt/authorEmail`。
  - 若該膠囊沒有對應 `square_post`，則停用「聊聊」與隱藏廣場互動（讚踩收藏評論），避免誤操作。
- 驗證：`npm run build` 通過。

---

### 互動輸入體驗與字數限制
- 需求：
  - 膠囊彈窗中的「膠囊私訊」輸入框僅在首次開線時顯示；開線後改到「我的 → 聊聊記錄」續聊。
  - 修正輸入框文字可讀性（白底需深色字）。
  - 撰寫／回覆／評論等內容統一限制約 300 字。
- 前端：`src/App.tsx`
  - 新增 `TEXT_LIMIT = 300`，並套用至主要內容輸入：
    - 撰寫內容、編輯內容、雙方往來、膠囊私訊、聊聊輸入、廣場評論。
  - 為白底輸入框補上 `text-stone-900` 與 `placeholder:text-stone-400`，避免看不清。
  - 在膠囊彈窗中新增「首次開線判斷」：
    - 非參與者且尚未開線時顯示私訊輸入框。
    - 已開線後改顯示導引按鈕，前往「我的 → 聊聊記錄」續聊。
    - 首次送出後自動跳到聊聊記錄。
- 後端：`spacetimedb/src/index.ts`
  - 新增 `MAX_MESSAGE_CONTENT_LEN = 300`。
  - 將以下內容長度驗證統一為 300 字：
    - `send_direct_message`
    - `send_capsule_message`
    - `update_scheduled_message`
    - `append_letter_exchange`
    - `add_square_comment`
    - `add_capsule_private_message`
  - 已發佈：`npm run spacetime:publish`
- 驗證：
  - `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
  - `npm run build`

---

### 膠囊彈窗版面重排（依最新 UI 需求）
- 檔案：`src/App.tsx`
- 調整：
  - 移除彈窗中多段提示文案（含私訊標題說明、未貼廣場提示、未開放評論提示等）。
  - 時間顯示規則：
    - 未開啟：顯示「預定開啟 + 時間」。
    - 已開啟：僅顯示時間，不再顯示「預定開啟」字樣。
  - 放大內容顯示區與私訊輸入框（內容區可捲動、輸入框提高為 4 行）。
  - 將主要操作按鈕移到底部固定區：
    - `送出私訊`
    - `換一個`
  - `換一則`新增刷新過場：
    - 點擊後顯示 `正在打開新膠囊…`
    - 內容區出現 loading 動畫，完成後載入新膠囊。
- 驗證：
  - `npm run build` 通過。

---

### 緊急修復：未貼廣場膠囊無法輸入/送出
- 問題：在「僅膠囊不上廣場」模式下，彈窗底部「送出私訊」按鈕未顯示，且送出時後端會被 `square_post` 檢查擋下。
- 修復：
  - `src/App.tsx`
    - 放寬底部 `送出私訊` 顯示條件，不再要求 `capsuleSquarePost` 存在。
  - `spacetimedb/src/index.ts`
    - `add_capsule_private_message`：若無 `square_post`，改直接以 `capsule_message` 作為來源做權限判斷與開線。
    - `capsule_private_for_me` view：若無 `square_post`，同樣以 `capsule_message` 判斷可見性，確保聊聊列表可讀。
- 已執行：
  - `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
  - `npm run build`
  - `npm run spacetime:publish`

### 補丁：私線入口顯示條件再修正
- 問題：膠囊彈窗仍可能因「參與者判斷依賴舊資料來源」導致輸入框不顯示。
- 調整：`src/App.tsx`
  - 私線參與者判斷改為優先使用 `capsule_message`（作者身份）與既有 `scheduled_message` 兼容判斷。
  - 首次開線輸入框條件移除 `square_post` 依賴，確保私線入口不再被廣場綁定。
  - 訪客線初始化改為同時支援 `capsule_message` 與 `scheduled_message` 來源。
- 驗證：`npm run build` 通過。

### 補丁2：私線輸入框/送出按鈕固定可見
- 問題：`capsule-modal-private-thread` 區塊仍綁到 `capsuleSquarePost`，在未貼廣場膠囊時可能不顯示輸入框與送出按鈕。
- 調整：`src/App.tsx`
  - 移除私線輸入框與底部 `送出私訊` 對 `capsuleSquarePost` 的依賴條件。
  - 補上區塊標籤文案：`私線訊息（不公開）`。
- 驗證：`npm run build` 通過。

---

### 新增：膠囊類型（僅膠囊）
- 需求：撰寫「僅膠囊」時可選 10 種類型（求助/吐槽/戀愛/閒聊/樹洞/分享/職場/治癒/八卦/建議）。
- 後端：`spacetimedb/src/index.ts`
  - `capsule_message` 新增欄位：`capsuleType: t.u8().default(4)`（表尾新增，遷移安全）。
  - `send_capsule_message` 新增參數：`capsuleType: t.u8()`，並驗證範圍 `1..10`。
  - `dev_seed_secret_capsule_posts` 補寫 `capsuleType`（依 slot 分配 1~10），避免 seed insert 型別缺欄。
- 前端：`src/App.tsx`
  - 新增 `CAPSULE_TYPE_OPTIONS` 與 `composeCapsuleType` state（預設 4=閒聊）。
  - 僅在 `composeMode === 'capsule'` 顯示「膠囊類型」下拉。
  - 呼叫 `sendCapsuleMessage` 時帶入 `capsuleType`。
- 產物同步與驗證：
  - `npm run spacetime:generate`
  - `npm run build`
  - `npm run spacetime:publish`
  - 雲端 migration 已建立 `capsule_message.capsule_type` 預設值 4。

---

### 聊聊視窗強化（聊天樣式與對方資訊）
- 需求：聊天視窗呈現左對方／右我方氣泡，底部輸入送出；補齊對方資訊與互聊進度。
- 調整：`src/App.tsx`
  - 聊聊清單與詳情的對方名稱/主文摘要改為優先使用 `capsule_message` 與 `account_profile`，不再只靠廣場快照。
  - 聊天訊息泡泡加上發話方標示（我 / 對方），維持左對方右我方。
  - 頂部卡片新增「互相聊進度：x/10」。
  - 新增「對方資訊」按鈕與資訊彈窗（暱稱、性別、年齡、自介）。
  - 空對話時顯示「先發第一句」提示。
- 驗證：`npm run build` 通過。

---

### 私線規則：前 10 句必須輪流
- 需求：打招呼後若對方未回覆，不可再連發；前十句必須你一句我一句。
- 後端調整：`spacetimedb/src/index.ts`（`add_capsule_private_message`）
  - 針對同一 `sourceMessageId + threadGuestIdentity` 計算 thread 已有訊息數與最後一則作者。
  - 當 thread 訊息數 `< 10` 且最後一則作者就是目前發送者時，拒絕送出：
    - 錯誤訊息：`前 10 句需你一句我一句，請等對方回覆`
  - 第 11 句起解除輪流限制。
- 已執行：
  - `npm exec tsc -- -p spacetimedb/tsconfig.json --noEmit`
  - `npm run spacetime:publish`

### 聊天區版面調整（滿版 + 底部固定輸入）
- 需求：聊天內容區盡量撐滿；輸入框與送出固定在底部一列；輸入框平時 1 行，最多展開 3 行。
- 調整：`src/App.tsx`
  - 聊天容器改為 `h-full + flex-col`，中間訊息區 `flex-1` 撐滿並可捲動。
  - 輸入列改為底部固定區塊（同卡片內底部）。
  - `textarea` 改為 `rows=1`，加入自動高度調整：
    - 初始高度 `44px`（一行視覺）
    - 最高 `84px`（約三行）
    - 超過後內部滾動。
  - 送出後重置輸入高度回一行。
- 驗證：`npm run build` 通過。

### 聊天頂部重設計（進度解鎖對方資訊）
- 需求：
  - 移除「聊聊對象」文字。
  - 右側改用圖示按鈕表示「對方資訊」。
  - 進度放在頂部區塊，10 句內顯示解鎖進度；滿 10 後才可看對方資訊。
- 調整：`src/App.tsx`
  - 新增 `chatPeerUnlocked = selectedChatProgress >= 10`。
  - 右側圖示按鈕邏輯：
    - 未解鎖：顯示愛心 icon（`Heart`）＋ `解鎖 x/10`，按鈕不可點。
    - 已解鎖：顯示個人資訊 icon（`User`）＋ `已解鎖`，可開啟對方資訊。
  - 頂部文案改為：`互聊進度：x/10`（保留主文摘要）。
- 驗證：`npm run build` 通過。

### 聊天頭部微調（主文入首句 + 愛心液位）
- 需求：
  - 主文不再放頭部摘要，改為聊天第一句。
  - 愛心去掉外層框，並以進度灌滿視覺呈現。
  - 文字改為「解鎖對方資料」。
- 調整：`src/App.tsx`
  - 移除頭部 `主文：...` 顯示。
  - 在訊息列表最上方插入一則固定首句氣泡（主文內容）。
  - 未解鎖圖示改為雙層 Heart + clipPath 液位填充（0~100% 對應 0~10 進度）。
  - 未解鎖文字改為 `解鎖對方資料 x/10`。
- 驗證：`npm run build` 通過。

---

### 膠囊聊聊跳轉體驗修正
- 需求：
  - 點「換一個」時清空當前私線輸入內容。
  - 從膠囊彈窗送出私訊後跳到「聊聊記錄」時，彈窗需同步關閉。
  - 在聊聊頁按返回時，需回到原本跳轉來源分頁。
- 調整：`src/App.tsx`
  - `pickAnotherCapsule` 補上 `setCapsulePrivateDraft('')`，切換膠囊時不帶入舊草稿。
  - 新增 `chatBackTab` 狀態，於 `jumpToChatFromCapsule` 記錄來源分頁。
  - 手機返回按鈕加入 chat 來源回跳邏輯：若存在 `chatBackTab`，返回即切回該分頁。
  - 在「我的」直接進入聊聊或手動選聊天線時，清空 `chatBackTab`，避免舊來源殘留影響返回行為。
- 驗證：`npm run build` 通過。

---

### 聊聊列表進入行為修正（不自動開第一則）
- 需求：進入「我的 > 聊聊記錄」時應先停留在聊天列表，不要自動展開第一條聊天。
- 調整：`src/App.tsx`
  - 調整 `activeTab === 'chat'` 的同步 `useEffect`：
    - 原本：若目前未選中聊天線，會自動選 `capsuleChatThreads[0]`。
    - 現在：僅保留既有且仍存在的選擇；若無有效選擇則維持 `null`（只顯示列表）。
  - 仍保留既有行為：若是從膠囊彈窗「聊聊」跳入，會帶入指定聊天線並直接開啟該線。

---

### 膠囊類型改為彩色 Tags + 膠囊固定即時送出
- 需求：
  - 撰寫膠囊時，類型改用 tag 選擇，且每種顏色不同（戀愛為粉色）。
  - 顯示膠囊內容時，也要帶同色類型標籤。
  - 膠囊不再選擇時間，預設即時送出並隱藏時間輸入。
- 調整：`src/App.tsx`
  - 新增膠囊類型色票：
    - `CAPSULE_TYPE_THEME`（一般顯示）
    - `CAPSULE_TYPE_ACTIVE_THEME`（撰寫時選中狀態）
    - `capsuleTypeMeta(...)` 統一回傳 label 與對應色彩 class。
  - 撰寫區「膠囊類型」由 `<select>` 改為可點擊的 `#tag` 群組。
  - 類型顯示同步套用到：
    - 「我的 > 聊聊記錄」列表項
    - 聊天視窗主文首句
    - 「秘密膠囊」抽取彈窗主文卡片
  - 送出流程調整：
    - `sendMessage` 僅在 `composeMode === 'direct'` 時解析 `scheduledAt`。
    - 膠囊成功訊息固定為「秘密膠囊已即時送出。」。
  - 表單顯示調整：
    - 膠囊模式隱藏 `datetime-local` 欄位，改顯示「膠囊固定即時送出」提示。
    - 送出按鈕文案在膠囊模式改為「送出膠囊」。
- 驗證：`npm run build` 通過。

---

### 新增個人空間（混合流）與膠囊刪除不斷聊
- 決策落地：
  - 個人空間不分「膠囊／牆」，改為單一時間流混合顯示。
  - 膠囊僅在「公開到個人空間」時可被他人看到，且他人可直接點膠囊發起聊聊。
  - 刪除膠囊後，既有聊聊紀錄保留（主文不清空）。
- 後端：`spacetimedb/src/index.ts`
  - `capsule_message` 新增欄位（表尾，遷移安全）：
    - `isProfilePublic: t.bool().default(false)`
    - `isDeleted: t.bool().default(false)`
    - `deletedAt: t.timestamp().optional()`
  - `send_capsule_message` 新增參數 `isProfilePublic`，插入時寫入公開狀態。
  - 新增 reducer：`delete_capsule_message`
    - 僅作者可刪。
    - 若有廣場貼文先 `deleteSquarePostCascade` 撤下公開。
    - 以軟刪除更新 `isDeleted/deletedAt`，不刪 `capsule_private_message`。
  - 同步補齊 seed insert 新欄位，確保 codegen/編譯通過。
- 前端：`src/App.tsx`
  - 新增「我的空間」卡片入口（Mine 卡片群）。
  - 新增 `activeTab = 'space'` 與空間 owner 狀態，支援查看自己或他人空間。
  - 新增混合流資料組合（`spaceFeed`）：
    - 膠囊（排除 `isDeleted`）
    - 廣場公開
    - 以 `createdAt` 倒序合併排序。
  - 他人空間僅顯示 `isProfilePublic` 膠囊；本人空間顯示自己的送出紀錄。
  - 本人空間膠囊卡片加入「刪除」按鈕（呼叫 `deleteCapsuleMessage`）。
  - 膠囊撰寫新增「公開到個人空間」開關，送出時帶入 reducer。
  - 在秘密膠囊彈窗／廣場詳情新增「看 TA 的空間」入口。
- 型別同步與驗證：
  - `npm run spacetime:generate`
  - `npm run build`
  - `ReadLints`（`App.tsx`、`spacetimedb/src/index.ts`）無錯誤。

---

### 管理能力基建 + 年齡強制補全（不可跳過）
- 後端：`spacetimedb/src/index.ts`
  - 年齡規則統一為 `16–126`。
  - `register_account` 改為 `ageYears` 可先留空（存 `0`）；`update_account_profile` 仍要求合法年齡。
  - 新增 reducer：`set_age_years`，供登入後強制補齊年齡流程使用。
  - 新增管理/舉報相關資料表：
    - `admin_role`
    - `admin_audit_log`
    - `report_ticket`
    - `report_target_snapshot`
    - `moderation_action_log`
    - `moderation_queue`
    - `user_sanction`
    - `appeal_ticket`
  - 新增 reducers：
    - `set_admin_role`
    - `create_report_ticket`
    - `admin_update_report_ticket`
    - `admin_apply_user_sanction`
    - `create_appeal_ticket`
    - `admin_update_appeal_ticket`
    - `restore_capsule_message`
  - 舉報目標支援：
    - `capsule`
    - `square_post`
    - `chat_thread`
    - `chat_account`
  - 舉報建立時落地目標快照，便於後續精準定位與審核。
- 前端：`src/App.tsx`
  - 登入後若年齡缺失/不合法，顯示不可關閉年齡 gate（全屏鎖定，僅可提交）。
  - 年齡以可捲動選擇（16–126）提交到 `setAgeYears`。
  - 註冊頁年齡改為可略過，文案提示「登入後必填」。
  - 新增舉報 modal 與入口：
    - 聊聊頁：檢舉帳號
    - 廣場詳情：檢舉貼文
    - 膠囊彈窗：檢舉膠囊
  - 舉報內容前端限制：`10–2000` 字。
- 發佈與驗證：
  - `npm run spacetime:generate`
  - `npm run spacetime:publish`（maincloud `idea-jd2zx` 發佈成功）
  - `npm run build`
  - `ReadLints` 無錯誤。

---

### 管理後台第一版（列表 / 審核 / 處分）已接線
- 前端：`src/App.tsx`
  - 「我的」頁新增管理入口：
    - 若目前身份為管理員，顯示「管理後台」卡片並可進入。
    - 若系統尚無管理員，顯示「建立首位超級管理員（自己）」入口。
  - 新增 `activeTab = 'admin'` 分頁：
    - 左欄：舉報單列表（狀態、原因、目標、優先級）。
    - 右欄：舉報詳情、目標快照、審核操作表單（狀態/優先級/處理說明）。
  - 審核操作接上後端 reducer：
    - `adminUpdateReportTicket`
    - `adminApplyUserSanction`
    - `setAdminRole`（首位管理員 bootstrap）
  - 對 `chat_account` 類型舉報提供快速處分按鈕（warn/mute/limit/ban）並同步結案。
  - 手機返回邏輯與列表狀態同步：
    - 管理分頁選中單據後可返回列表；
    - 切換各分頁時清理管理分頁選中狀態，避免殘留。
- 型別與建置驗證：
  - `npm run build` 通過（`tsc` + `vite build` 成功）。

---

### 管理帳號位置與補全（可見化 + 管理名單）
- 現況確認（maincloud）：
  - `spacetime sql idea-jd2zx "SELECT admin_identity, role, is_active FROM admin_role" --server maincloud`
  - 查詢結果為空，代表目前尚未建立任何管理帳號。
- 前端補全：`src/App.tsx`
  - 「我的」頁固定顯示「管理後台」卡片（不再只有管理員看得到）。
  - 進入 `管理後台` 後，未授權使用者也能看到目前管理帳號清單（若有）。
  - 管理端主區新增「管理帳號」區塊：
    - 顯示啟用中的管理員清單（role + identity）。
    - `super_admin` 可直接輸入 identity、設定 role 與啟用狀態，呼叫 `setAdminRole` 進行新增/更新。
  - 系統尚無管理員時，維持「建立首位超級管理員（自己）」快速入口。
- 驗證：
  - `npm run build` 通過。

---

### 管理帳號進一步補全（搜尋 + 一鍵停權/復權）
- 你提問「19 個表是否正常」：
  - 目前 `schema({ ... })` 內宣告為 19 張表，屬於預期狀態（含管理/舉報/申訴相關）。
  - `SHOW TABLES` 在目前 maincloud SQL 介面不支援（回傳 `Unsupported: SHOW TABLES`），改以後端 schema 與實際查表交叉確認。
  - `admin_role` 目前查詢為空（尚未建立管理帳號）。
- 後端：`spacetimedb/src/index.ts`
  - 新增 reducer：`admin_set_user_sanction_status`
    - 允許管理員把 `user_sanction.status` 在 `active` / `revoked` 間切換。
    - 寫入 moderation/admin audit log，供後續追蹤。
- 前端：`src/App.tsx`
  - 管理後台新增「帳號搜尋與一鍵停權 / 復權」區塊：
    - 可用暱稱 / 信箱 / identity 搜尋帳號並選中目標。
    - 顯示目標帳號目前啟用處分數與 ban 數。
    - 一鍵停權：快速建立 `ban` 處分。
    - 一鍵復權：將最新啟用中的 ban 處分改為 `revoked`。
  - 管理入口維持在「我的」固定可見，方便快速定位。
- 產物同步與驗證：
  - `npm run spacetime:generate`
  - `npm run build`（通過）
  - `npm run spacetime:publish`（maincloud `idea-jd2zx` 更新成功）

---

### 補齊需求 7/8（我的個資入口與自介獨立編輯）
- 需求 7：點擊個人卡片頭像/資訊區，先進入「編輯選單」，再選擇操作。
  - 前端：`src/App.tsx`
    - 新增 `profileActionMenuOpen` 動作選單彈層。
    - 在「我的」個資卡將頭像+資訊區改為可點擊入口。
    - 選單項目：
      - `基本資料`（開啟原本個資編輯）
      - `修改秘密`（新密碼修改頁）
      - `登出`
- 需求 8：自我介紹外層區塊加修改 icon，獨立進入編輯頁，完成後返回。
  - 前端：`src/App.tsx`
    - 在個資卡新增「自我介紹」區塊與 `修改` icon 按鈕。
    - 新增 `introEditOpen` 編輯彈層，儲存後自動關閉返回。
    - 自介前端限制：`10–400` 字。
- 同步補齊「修改秘密」後端能力：
  - 後端：`spacetimedb/src/index.ts`
    - 新增 reducer：`change_password`（舊密碼驗證 + 新密碼更新）。
  - 產物：
    - `npm run spacetime:generate` 產生 `change_password_reducer.ts`。
- 自介字數規則後端對齊：
  - `validateProfileFields` / `validateProfileFieldsRegister` 新增最小長度規則（有填寫時至少 10 字，上限 400 字）。
- 驗證與發佈：
  - `npm run spacetime:generate`
  - `npm run build`
  - `npm run spacetime:publish`（maincloud `idea-jd2zx` 成功）
