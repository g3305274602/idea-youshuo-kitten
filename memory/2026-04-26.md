# 2026-04-26

## 手機底欄結構重構（圓角底欄＋中央外凸 FAB）

- **目的**：對齊稿圖（左撰寫、中間黃圓膠囊、右我的），中央鈕半圓突出底欄上緣；**移除** `clipPath`＋`ys-mobile-bottom-nav-bubble` 氣泡層、以及 `-translate-y-6` 等與上緣裁切/定位打架的寫法。
- **`MobileBottomNavSection`**：外層 `<nav>`；`ys-mobile-bottom-nav-shelf` → `ys-mobile-bottom-nav-rail`（`relative`）內：  
  - `ys-mobile-bottom-nav-bar`：`flex` 三欄，左右 `ys-mobile-tab`（`flex-1`），**中欄** `ys-mobile-bottom-nav-fabSlot`（`w-14` + `self-stretch`）：內層即 `ys-mobile-capsule-button`，`absolute` 在 slot 內 `left-1/2 top-0 -translate-x-1/2 -translate-y-1/2` 讓鈕一半高於欄上緣；`drop-shadow` 仍用 `ys-mobile-capsule-button-img`（`index.css`）。
- 圓心對齊底欄 **上緣**，故視覺上「冒出」一截；`MobileBottomNavSection` 仍掛在 `SpacetimeMailboxApp` 根層**外**、避開內層 `overflow-hidden` 裁切（見下節歷程）。

## 手機底欄抽膠囊圖

- `src/assets/images/app/secret/capsule-draw-fab2.png`：**手機底欄**中央「抽取膠囊」鈕用圖（`MobileBottomNavSection`），由 Vite 打包；可替換同名檔（建議含透明底與週圍光暈的 PNG）。
- `capsule-draw-fab.png`：秘密分頁主 CTA（`SecretCapsuleDrawButton` / `treasure`）仍用此檔，與底欄圖可分流。
- 底欄膠囊鈕：鮮黃漸層圓底（`#ffeb7a`→`#f4dc3a`→`#e2b820`）＋白邊內陰影／外光暈，與稿圖一致；`h-12`、內圖 `h-7`。
- 底欄外框：`ys-mobile-bottom-nav-shelf`＋`ys-mobile-bottom-nav-bubble`；`clip-path: url(#id)`＋`<clipPath clipPathUnits="objectBoundingBox">`；中央黃鈕 `h-14`、內圖 `h-9`、`-translate-y-6`；氣泡 `-top-6`、`inner` `pt-3`。
- **根因與修復**：`SpacetimeMailboxApp` 根層有 **`overflow-hidden`**，會把底欄內**向上平移**的膠囊鈕與弧邊一併裁掉，看起來像平頂黑條、鈕卡在欄內。已把 **`MobileBottomNavSection` 改為與根層 `</div>` 同層的兄弟**（外層 `<>...</>`），不放在 `overflow-hidden` 內，凸起與外冒才可見。
- 底欄中央膠囊鈕：`h-10` 黃圓＋內圖 `h-6`；多層 `box-shadow` 做**柔黃外光暈**（`rgba(255,220,120)`～`255,170,40` 漸層擴散）。

## 秘密分頁主畫面：抽膠囊 CTA 圖

- **`components.SecretCapsuleDrawButton`**、`variant="treasure"`：中央大膠囊與底欄共用 **`capsule-draw-fab.png`**（取代原 CSS 膠囊＋蓋子動畫），保留點擊前短延遲與 `capPulled` 縮放／旋轉回饋；`SecretMainSection` 與 `SpacetimeMailboxApp` 秘密分頁空態皆用此變體。

## 靜態資源目錄

- 建立改版用靜態資源結構：`src/assets/images/app/{mine,chat,secret}`、`images/{common,temp}`、`icons/{ui,feature}`、`illustrations`、`fonts`、`lottie`；空目錄以 `.gitkeep` 納入版本庫。
- `public/og`、`public/share` 供固定 URL 資源；`docs/design-refs` 供設計參考稿（不進打包引用）。
- `src/assets/README.md` 簡述用途與命名建議。

## 夢幻星空毛玻璃介面（續）

- **膠囊全屏彈窗**：`ys-capsule-backdrop` 背景加深、模糊 22px；`ys-capsule-sheet` 維持深色漸層以維持既有青色系內文可讀性；`CapsuleOverlaySection` 加入 `ys-capsule-drag` 拖曳條手柄。
- **全頁載入**：`SpacetimeMailboxApp` 載入中容器改 `ys-dreamy-cosmic`（與主題一致，移除 `#2a5e59`）。
- **CSS（Tailwind v4）**：`@apply` 不可引用自訂類名；已將 `ys-mine-profile-card`、`ys-mine-menu-shell` 內的 `@apply glass-effect` 改為等價屬性；`ys-mobile-capsule-button` 的 `@apply ys-gold-glow` 改為內聯 `filter: drop-shadow(...)`。
- **手機底欄抽膠囊鈕點按**：勿在鈕上用 `active:translate`（會覆寫置中 `transform`）。**下壓**改為僅在 `fabSlot` 內：`transition-[top]`，`:active` 時 `top: 0.2rem`；另保留內層圖 `group-active:scale-95` 疊加回饋。
- **底欄膠囊鈕光暈**：預設僅內陰影＋淺邊；`activeTab === "secret"` 時加 `ys-mobile-capsule-button--on-secret`，才顯示外層黃／粉光暈；`transition-[box-shadow]` 切換分頁時較順。
- **手機底欄左右**：`PenTool`→`SquarePen`，`User`→`CircleUser`；去除「撰寫」「我的」字樣，僅圖示。`index.css` 的 `.ys-mobile-tab` 改為僅在 `.ys-mobile-bottom-nav-bar` 內佈局，移除 `ys-mobile-tab-label`。
- **左欄選中描邊捲動裁切（多處）**：`ChatThreadsSidebarSection` 改 **`ring-inset` violet**＋列表包 **`px-0.5 pb-3`**；`MailboxGroupedList`／`MessageTile` 選中改 **`ring-inset`**，最外層加 **`px-0.5 pb-3`**（與藏心底一致）。
- **藏心底左欄選中外框被裁切**：`FavoritesSidebarSection` 的 `ring-2` 在按鈕外側，捲動層 `overflow` 會切掉邊；改 **`ring-inset`** 並列表外包 **`px-0.5 pb-3`**。
- **廣場牆左欄誤佔全寬**：`mine_square` 未選貼文時不應與 `secret` 一併用 `md:w-full`（主欄廣場牆會被擠出）；改為僅 `secret && !squareSelectedPostId` 時全寬左欄。
- **廣場牆（`mine_square`）桌機佈局**：未選貼文時 `SecretWallSection` 改在**主欄**全寬顯示（`max-w-5xl` 置中、列表區 `max-h` 加大），左欄僅導列；手機仍只在左欄牆。已選貼文時左欄恢復牆列表、主欄看 `SecretMainSection`；共用 `mineSquareSecretWallCommon`。
- **「我的」桌機導列含廣場／空間**：`MineHubMenuNav` 第一列為 **廣場牆**（`mine_square`），其餘含 **我的空間**；`showMineHubDesktopSubNav` 納入 **`mine`**／**`mine_square`**，桌機進「我的」即見整條導列，無需先點雙卡。手機仍用 `inShell` 全列（`md:hidden` 與桌機頂列分工）；已移除雙大卡、刪未使用之 `onMineOpenSquareWall`；廣場列圖示用 **`.ys-mine-menu-icon--lilac`**。
- **桌面版「我的」子頁左欄選取看似消失**：從大卡片點入 `inbox`／`outbox`／`chat`／`space`／`favorites` 後，左欄改為子列表導致五入口被替換。已抽出 **`MineHubMenuNav`**，在 **md+** 當上列任一分頁作用中時，於左欄**列表上方**再顯示一條五入口（`layout="desktop"`、較緊字級），與主列表共用滾動區外層的 `shrink-0`；`activeTab` 對應列加 **`.ys-mine-menu-row--active`**（金黃條＋淺底）。大卡片內清單改為同元件 `inShell` 並傳 **`activeTab`** 以在「我的」根頁也一致。
- **底欄左右 `.ys-rail-squ` 陰影**：`box-shadow` 整體減重（內陰、厚底、外擴散皆降不透明度與模糊半徑；選中態由雙層色底＋大暈改為單層邊＋較輕外光），仍保留一點立體但較不搶眼。
- **手機底欄下緣內邊距**：`ys-mobile-bottom-nav-bar` 之 `pb` 改為 **`max(0.875rem, env(safe-area-inset-bottom, 0px))`**，並於 **`index.html` viewport 加 `viewport-fit=cover`** 以利 iOS 讀取 safe area；`SpacetimeMailboxApp` 主內容區之 `pb` 改 **`calc(5.5rem + env(safe-area-inset-bottom,0px))`**，與加高後的底欄對齊，避免列表最底被擋。
- **手機底欄左右圖示（續）**：`MobileBottomNavSection` 左右外層改套 **`ys-rail-squ` + `ys-rail-squ--write`／`--me` + `ys-rail-squ--on`／`--off`**（`index.css` 軟立體貼紙塊：內陰＋厚底陰＋邊；選中為粉紫／粉橘＋金糖漸層＋下壓陰影）；未選中圖示略放大描邊。底欄「我的」**未讀紅點**加 `z-10`＋`pointer-events-none` 免被方塊蓋住或誤觸。
- **`components.MineProfileSummaryCard`**：改為與 `ys-mine` 風格一致（毛玻璃卡、`ys-mine-avatar-slot`、白字層級）。
- **`TopNavSection` 桌面使用者下拉**：改 `glass-effect` 深色層、按鈕金黃/玫瑰語意、列表 hover 用 `hover:bg-white/10`。
- **建置**：`npm run build` 已通過。

## 我的：廣場牆＋我的空間捷徑

- **取代** 原 `ys-mine-stats-row` 四欄（飄向我的／我丟出的／聊聊／收藏數字列）。
- **兩欄並排**（`grid-cols-2` + `glass-effect`）：**廣場牆**（`LayoutGrid`）與 **我的空間**（`Home`），副文案為短介紹；下方選單列仍保留飄向我的等入口。
- **廣場牶**：`squarePostsCount` 使用 `squarePostsSorted.length`；點擊觸發 `onMineOpenSquareWall` → `mine_square` 並清選中狀態（見下節）。
- **我的空間**：等同選單「我的空間」，`onNavigate("space")`（含既有 `setSpaceOwnerHex` 邏輯）。

## 秘密分頁側欄：聊聊＋新訊息氣泡＋返回

- **廣場牆**改由「我的」與 `mine_square` 等入口；秘密側欄**不再**放廣場跳轉，改為**聊聊**（`MessageCircle` 綠色漸層區塊）。
- 點擊呼叫 `onSecretSidebarOpenChat`：設 `chatBackTab` 為 `"secret"` 後 `setActiveTab("chat")`，讓**從此入口進聊聊列表**時，手機返回／頂欄能回到**秘密**（`useNavigationActions.onMobileBack` 優先處理 `activeTab === "chat" && !selectedChatThreadKey && chatBackTab`）。
- **氣泡**：`secretChatNewThreadCount` 依每條聊聊線最後一則私訊**是否為本人所發**（`authorIdentity`）計有「對方新訊息待看」的線程數；`>0` 時右側小圓紅色 badge（`>99` 顯示 `99+`）。
- **手機底欄「秘密」**（`onGoSecret`）會一併 `setSelectedChatThreadKey(null)`、`setChatBackTab(null)`，避免從底欄回秘密仍帶著「應回秘密的聊聊路徑」狀態。
- **建置**：`npm run build` 通過；私訊列型別使用 `authorIdentity`（非 `senderIdentity`）；避免 `Array#at` 以符合現有 `lib` 設定。

## 聊聊：作者看訪客顯示「訪客 c200bada…」

- **原因**：一般用戶只訂閱 `accountProfile.where(自己)`，`useAdminWorkbench` 的 `profileByIdentityHex` 也僅含自己；膠囊作者從 `threadGuestIdentity` 查訪客 `displayName` 永遠 miss，只能顯示 `訪客 ${hex}…`。
- **處理**：`SpacetimeMailboxApp` 另訂閱**完整** `tables.accountProfile`（後端本為 `public: true`），建 `publicProfileByIdentityHex`；`capsuleChatThreads` 作者分支、聊聊對手檔 `selectedChatPeerProfile` 及「我的空間」頭像用 `publicProfileByIdentityHex`（必要時回退舊 map）。

## 聊聊：主文氣泡區分「我發的膠囊」與「對方主文」

- **判定**：私訊線以訪客 `threadGuestHex` 為 key；`threadGuestHex !== identity` 時目前使用者為膠囊作者（主文為本人）。
- **UI**：`ChatMainSection` 傳入 `isSourceCapsuleMine`；主文區塊為本人時用 `ys-chat-bubble-self`（右側黃色）、標題「我的膠囊主文」＋類型 chip；為對方主文時維持 `ys-chat-bubble-peer`（左側）＋對手暱稱。

## 聊聊／抽膠囊彈窗：解鎖前匿名稱呼、彈窗 UI

- **`helpers.anonPaperNoteLabel(gender)`**：未解鎖時依性別顯示「一個女孩的紙條」等（`other`／未填另有文案）。
- **`CapsuleChatThreadSummary.threadPrivateMessageCount`**：每線私訊則數；`>=10` 與 `chatPeerUnlocked` 一致視為解鎖。
- **聊聊頁**：頂欄、對方主文氣泡、左側線程列表在解鎖前用匿名稱；解鎖後顯示真暱稱與性別圖示；解鎖後新增「TA 的空間」按鈕（`onOpenChatPeerSpace` → `openSpace`）。
- **抽膠囊彈窗**：移除「TA 的空間」；作者名在 `capsuleOverlayShowAuthorRealName`（本人為該信參與者或該線已滿 10 則私訊）前用匿名稱；私訊輸入改單行；底欄「換一個」與「送出私訊」**左右互換**且**樣式對調**（左大格深色、右黃色送出）。

## 秘密分頁側欄的廣場牶（歷程）

- 原內嵌 `SecretWallSection` 已先改**單一跳轉**（`onMineOpenSquareWall` → `mine_square`）；後續廣場改由我的／`mine_square` 負責，秘密側欄則改為上節的「聊聊」入口。

## 廣場牶二級頁（`mine_square`）

- 新增 `AppTab`：`mine_square`。從「我的」點「廣場牆」改為 **`setActiveTab("mine_square")`**，不再切到 `secret`、也不操作 `secretWallExpanded`。
- 側欄僅 **廣場牆**（`SecretWallSection` 常駐展開、`onToggleExpanded` 空操作）；主欄未選貼文時顯示短說明；選取貼文後與秘密分頁相同使用 `SecretMainSection`。
- 導航：`MINE_SUB_TABS` / `MOBILE_BACK_BUTTON_TABS` / `computeIsMobileDetailView` 納入 `mine_square`；`BackTab` 增 `mine_square`；手機頂欄「返回」先清貼文再回「我的」；底欄「我的」在 `mine_square` 仍為高亮。
- `capsuleUiPostId`、廣場選中同步 `useEffect` 在 `mine_square` 與 `secret` 同邏輯。

## Hooks 修正

- `onMineOpenSquareWall` 的 `useCallback` 原置於 `isBooting` / 登入 early return **之後**，導致首屏與進入儀表板時 hooks 數量不一致。已**上移至** `if (isBooting)` 之前（與檔內 `unifiedFavoriteItems` 註解同一原則）。
- 新增 Cursor 規則：`.cursor/rules/react-hooks-mailbox.mdc`。

## Cosmic Drift 全專案 UI 改版（第一階段）

- **規範**：`.cursor/rules/cosmic-drift-design.mdc`（`alwaysApply: true`）；主色 `#FFD54F`、次色 `#F06292`、背景 `#121319`、容器 `#1A1B22`、輔助字 `#8E8E93`；字體改 **Plus Jakarta Sans**（`index.css` Google Fonts）。
- **`src/index.css`**：`@theme` 的 `--color-ys-*` 對齊新色；`ys-dreamy-cosmic` 改深空＋星雲微光；`glass-effect`、`ys-chat-*`、`ys-capsule-*`、`ys-mine-*`、`ys-mobile-bottom-nav-*` 等由青綠水色系改為白邊＋`#1A1B22` 表面；`body` 預設深底白字。
- **連線中畫面**：`App.tsx` 背景與 loader 用 Cosmic 主色。
- **全站主色字串**：各 section／元件中硬編碼舊金 `#f4dc3a` 改 `#FFD54F`。
- **青色系 class**：`Compose`／`Secret`／`Chat`／`CapsuleOverlay` 等之 `text-cyan-*`／`border-cyan-*`／`bg-cyan-*` 改為白／`#8E8E93`／`white/10` 等（與 `index.css` 的 `.ys-night-*` 一致）。
- **`MineHubBigCards` + `DashboardCard`**：由淺色漸層大卡改 **深空卡面**（`#1A1B22`）＋分區色邊（金／星雲粉／綠等）；圖示仍用漸層圓底。
- **`constants.ts` 膠囊類型 chip**：`CAPSULE_TYPE_THEME` / `CAPSULE_TYPE_ACTIVE_THEME` 改暗色可讀樣式。
- **建置**：`npm run build` 通過。

## Cosmic Drift 補齊遺留（郵票／淺色彈窗）

- **`index.css`**：新增 `cd-modal-panel`、`cd-field`、`cd-btn-ghost`、`cd-btn-primary`、`cd-btn-danger`、`cd-card-raised`（深空面＋細邊，取代 `border-stone-900`＋`#fffef7` 郵票風）。
- **撰寫頁**（`ComposeMessageMainSection`）：主表單漸層、已開信／廣場／往來摘錄／信封區與按鈕改 Cosmic；`MetaItem` 改白字＋`#8E8E93` 標籤（`components.tsx`）。
- **全站彈窗**：`Overlay`（對方資訊、刪除確認、舉報、封禁通知）、`Account`／`Profile`／`ProfileAction`／`AgeGate`／`PublishModal` 深空化；`PublishModal` 選項改粉／綠邊＋`#1A1B22`。
- **收藏／空間**：`FavoritesMainSection`、`SpaceMainSection`、`SpaceSidebarSection` 卡片與按鈕；`PickerControls` 滾輪深色＋`#FFD54F` 選中列。
- **管理後台**：`AdminSection` 管理帳號白底區塊、舉報／新增／編輯彈窗與列表列改深底；**登入** `AuthSection` 背景改 `#121319`＋星雲光暈＋副標主色黃。
- **側欄文案**：`FavoritesSidebarSection`、`ChatThreadsSidebarSection` 標題色改白。
- **建置**：`npm run build` 通過。

## 收藏／聊聊：未解鎖不顯示對方聯絡方式＋Cosmic 側欄

- **`UnifiedFavoriteListItem`** 新增 **`peerIdentityUnlocked`**：以該筆的 `sourceMessageId`（廣場為 `postSourceMessageId`、膠囊為 `capsuleId`）在 **`capsuleChatThreads`** 內尋找「本人參與的線程」且 **`threadPrivateMessageCount >= 10`** 則為 `true`（作者／訪客分支與主流程一致）。
- **`FavoritesSidebarSection`**：列表列改 `border-white/10`＋`#1A1B22` 深底；**未解鎖**只顯示 `anonPaperNoteLabel`＋`GenderIcon`；膠囊解鎖後才顯示 `snapshotAuthorEmail`；廣場未解鎖不呼叫 `squareAddressSubtitle`（避免信箱），改「來自廣場」＋匿名稱呼。
- **`ChatThreadsSidebarSection`**：同上深底樣式，副文改淺色字，空態 md 不黑字。
- **`ChatMainSection`**：無選中線程空態改深空用色；**`OverlayModalsSection` 對方檔案**：`isChatPeerProfileVisible`／`selectedChatPeerProfile` 在 **`chatPeerUnlocked === false` 時不顯示**（雙重保險）。
- **註解**：`SpacetimeMailboxApp`／`helpers`／`types`／`MobileBottomNavSection` 等刪減地圖式與贅字註解，保留需給未來維運的極少數（如 `Rules of Hooks` 一句）。

## 收藏「發起聊聊」：尚無私訊也能進入聊聊主欄

- **`selectedChatThread`**：若 `selectedChatThreadKey` 為 `sourceId::訪客hex` 但 **`capsuleChatThreads` 尚無該線**（首則私訊前），則從 `capsuleMessageRows`／`squarePostRows` 組出 **`CapsuleChatThreadSummary` 補位列**（`threadPrivateMessageCount: 0` 等），使 **`ChatMainSection`** 與 **送出** 可運作。
- **聊聊分頁 `useEffect`**：當 `capsuleChatThreads.length === 0` 時**不再** `setSelectedChatThreadKey(null)`。另：若**已有其他聊聊線**但當前 key 是「從收藏發起、尚未有私訊」的補充線，**列表內還不會有該 key**——邏輯改為：除 `capsuleChatThreads` 內有該 key 外，若 `key` 為 `sourceId::本人hex` 且 `sourceId` 在膠囊或廣場貼上仍可查得，**仍保留選取**，手機主欄才會進入 `isMobileDetailView` 顯示 `ChatMainSection`。
- **`jumpToChatFromCapsule`**：另 `setFavoriteSelectedId(null)`、`setSelectedMessageId(null)`，切到 `chat` 時主內容為聊聊。

## 可點列表列：`ys-tap-list-row`

- **`index.css`**：`.ys-tap-list-row { min-h-14 }`（約 56px，便於手指點擊）。
- **套用處**：`ChatThreadsSidebarSection`（並 `flex items-center`）、`FavoritesSidebarSection` 兩種收藏列、`components.MessageTile`、`SecretPaperListItem`、`MyReportsSidebarSection` 列、`AdminSection` 舉報列表按鈕。

## 聊聊側欄：列表不外露主文與最後內文

- **`CapsuleChatThreadSummary.hasNewMessageFromPeer`**：最後一則私訊之 `authorIdentity` 非當前使用者則 `true`（與秘密側欄新訊息計算一致；側欄列本身可不顯示此欄位）。
- **`ChatThreadsSidebarSection`**：不顯示主文、不顯示膠囊類型 **#樹洞** 等 tag（此列表單一語境無需多類型）；一列 **左**：匿名稱呼／解鎖後暱稱＋可選性別圖；**右**：`threadPrivateMessageCount <= 10` 時 **「n則訊息」**（灰字），`> 10` 時 **最後一則內文** `lastBody`（白字、truncate）。已移除 `capsuleTypeMeta` 傳參。

## 聊聊未讀提示（已讀游標）

- **`chatReadCursors.ts`**：`localStorage` 鍵 `youshuo_chat_read_cursor_<identityHex>`，每條 thread `key`（`sourceMessageId::threadGuestHex`）儲存已讀到的**最大訊息時間**（micros bigint）。
- **`useChatReadCursors.ts`**：載入／更新游標圖、提供 `markThreadRead`。
- **未讀定義**（`computeThreadUnread`）：最後一則為**對方**且時間 **>** 已讀游標；在 **聊聊分頁** 且當前線程訊息已訂閱時，以 `maxMessageMicros(selectedChatMessages)` 寫入游標（已讀到當下最新）。
- **UI**：`capsuleChatThreadsWithUnread` 帶 `hasUnread`；`ChatThreadsSidebarSection` 顯示小紅點；秘密側欄「聊聊」入口用 **有未讀的線程數** `chatUnreadThreadCount` 與文案；`MineSidebarSection`「聊聊記錄」僅在 `chatUnreadCount > 0` 顯示角標數；`MobileBottomNavSection`「我的」圖示在 `showMineChatUnread` 顯示小紅點。已移除以「最後一則是否為對方」粗算的全局計數，改以游標為準。
- **`MineSidebarSection`「藏心底」**：不顯示右側數量角標（`count: null`），已移除 `favoriteCount` 參數。
- **`CapsuleOverlaySection`**：移除彈窗內私訊歷史列表（與「聊聊」重複、常為空），並刪除 `capsulePrivateThreadMessages` 與 `SpacetimeMailboxApp` 對應 `useMemo`。
- **`ChatMainSection`**：訊息列表容器 `ref` + `useLayoutEffect`，在切線程（`key`）或訊息變化時 `scrollTop = scrollHeight`，預設檢視最底部最新訊息。
- **飄向我的／我丟出的未讀**：`mailboxReadCursors.ts` + `useMailboxReadState`（localStorage `youshuo_mailbox_read_<identity>`，inbox/outbox 各一 `…WatermarkMs`）；`createdAt` 大於游標計未讀；切到 `inbox`／`outbox` 分頁時游標拉至列表 `max(createdAt)`；首次無存檔時以目前列表 max 寫入避免舊信全顯未讀。`MineSidebarSection` 僅在未讀 >0 顯示角標。
- **未讀視覺**：`index.css` 新增 `.ys-unread-pill`（祕密側欄聊聊數字）、強化 `.ys-mine-menu-count`、`.ys-unread-dot`（列表小圓點＋手機底欄「我的」紅點），漸層玫紅→橘＋外光暈。

## 廣場牆（SecretWallSection）版式

- **`components.tsx`／`index.css`**：以 `ys-square-wall` 系列取代單一 `glass-effect` 區塊；背景漸層＋兩顆霧面光點、標題列帶圖示（`MessageSquare`）、`N 則` 琥珀小 pill、三行副標與展開狀態說明。
- **單則紙條**（`ys-square-wall-item`）：左緣漸層色條、選取時邊框／ring 強調；內文區：上牆／預定兩行時間、較大 `line-clamp-3` 預覽、關係行、底欄圓形作者鈕與讚／留言小 chip（琥珀／天青）。
- **空狀態**：`Sparkles` 虛線框＋兩行說明；列表 `space-y-2.5`；`SecretPaperListItem` 補 `Enter/Space` 鍵盤、移除未使用 `LayoutGrid` import。

## 我的子分頁左欄：導列上方「加一顆」返回（保留其餘）

- **我的子分頁左欄「返回」與空間同位置**：`MineHubMenuNav` 下方、捲動內最頂；`onAsideBackToMine`；`SpaceSidebarSection` 僅旅人卡＋數字。側欄返回後改 **`ys-mine-back-pill`／`__btn`** 雙層圓角軌＋內層漸層，與頂欄主分頁成組。頂欄主分頁用 **`ys-topnav-tablist`**＋**`ys-topnav-navtab`**；使用者名下拉鈕用 **`ys-topnav-user-pill`**。**檔案**：`index.css`、`components.tsx`（`NavTab`）、`TopNavSection.tsx`、`SpacetimeMailboxApp.tsx`。
- **側欄「返回我的」僅 `md+` 顯示**（`hidden md:block` 包住），手機不顯示、仍依頂欄／手勢等既有返回。

## 秘密分頁（`secret`）桌機版兩欄

- **左欄（`md+`）**：與「撰寫」相同結構的 `glass-effect` 介紹卡（`Sparkles`、標題「秘密」、一則說明），**不再**在桌機放抽膠囊＋聊聊。
- **手機（`<md`）**：左欄維持原樣（抽一則膠囊＋聊聊）。
- **主欄（`md+`）**：在 `SecretMainSection` 上方多一列與手機同款的「聊聊」大鈕（原從左欄移過去）；牆＋揀膠囊仍在 `SecretMainSection`。**檔案**：`SpacetimeMailboxApp.tsx`。（後續已改廣場／聊聊底列＋牆面跳轉，見實作。）

## 頂欄主分頁再精緻化

- **`ys-topnav-tablist`**：玻璃底（`bg-[#0c0c10]/75`＋`backdrop-blur-md`）、雙內陰＋上緣 1px 高光、細 ring。軌高 **`h-11`**、`p-1.5`。
- **`ys-topnav-navtab`**：13px、字距 `0.02em`、**選中**漸層＋**淡金 1px 外光**（`255,213,79` 12%）＋內上高光陰影；**未選** `text-white/40`、hover 明顯；`duration-300 ease-out`。
- **`ys-topnav-user-pill`**：與之呼應的內陰＋`backdrop-blur-sm`＋內 1px 高光。主分頁文案「撰寫」正體化。**檔案**：`index.css`、`TopNavSection.tsx`。

