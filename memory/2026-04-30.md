# 2026-04-30

## 頭像解鎖：確認框與「僅解鎖、不切換頭像」

- **問題**：付費解鎖確認層過寬；顯示 `xl-3` 等內部編號；解鎖後關閉整個頭像選擇窗並自動套用頭像。
- **後端**：`unlock_avatar_item` 移除對 `account_profile.avatar_key` 的更新，僅寫入解鎖記錄與扣點（若尚未擁有）；顯示頭像仍由 `set_avatar_key` 負責。
- **前端**：
  - `AvatarPickerModalSection.tsx`：確認卡片 `max-w-[min(100%,17.5rem)]`；標題用系列中文名（`seriesDisplayName`）；移除編號文案；整合「需要／目前」積分為一行說明；成功解鎖後只 `setConfirmKey("")`，頭像選擇窗維持開啟；按鈕內 await `onUnlockAvatar`。
  - `SpacetimeMailboxApp.tsx`：`handleUnlockAvatar` 不再 `setAvatarPickerOpen(false)`；免費邊際提示改為「已解鎖此張頭像，可點選格子套用…」；解鎖失敗時 `throw` 以利子層不清除確認層。
- **部署**：需重新發布 SpacetimeDB 模組後客戶端行為才與後端一致。

## 聊聊私訊氣泡誤判為「自己」（右側）

- **原因**：`ChatMainSection` 僅用 `authorAccountId === myAccountId` 判斷是否本人；若帳號欄位與預期不一致，對方訊息會被當成自己而靠右。
- **調整**：
  - `chatReadCursors.ts` 改為僅以 `authorAccountId` / `myAccountId`（雙側 `trim`）判斷本人訊息：`isChatMessageFromSelfByAccount`。
  - `computeThreadUnread` 同步改用帳號判斷，避免氣泡與未讀規則不一致。
  - `ChatMainSection` 氣泡左右改用 `isChatMessageFromSelfByAccount`，不再依賴 identity 欄位。

## 回覆他人膠囊時主文方向誤判

- **原因**：`isSourceCapsuleMine` 原本用 `threadGuestHex !== identity` 推導，會把「回覆他人膠囊」情境誤判為自己的主文，導致主文卡片顯示在右側。
- **調整**：改為以來源資料的 `accountId` 判斷：
  - 先找 `sourceMessageId` 對應的 `capsuleMessage.authorAccountId` 或 `squarePost.publisherAccountId`
  - 再與 `myAccountId`（`trim` 後）比較，僅相等時視為自己的主文。

## 聊聊前 10 句互鎖（雙方都送不出）

- **現象**：在部分線程中，前 10 句規則下雙方都被擋，顯示「請等對方回覆」而無人可發。
- **原因**：先前將輪流判斷改成純 `authorAccountId` 後，遇到舊資料 `accountId` 為空值，會把雙方誤判為同一人。
- **調整**（`spacetimedb/src/index.ts`）：
  - 參與者判定：膠囊來源先比 `authorAccountId`（雙方非空才比較），若缺值則回退以 `authorEmail` 比對。
  - 前 10 句輪流判定：先用非空 `authorAccountId` 比較；若缺值則回退 `authorIdentity` 比較，避免空值導致互鎖。

## 跨設備重登後私線偶發送不出＋私線積分規則調整

- **跨設備根因**：訪客路徑原本僅允許 `threadGuestIdentity === ctx.sender`，重登後 identity 變更會被拒（即使是同一帳號）。
- **後端修正**（`add_capsule_private_message`）：
  - 訪客身份判斷改為：`threadGuestIdentity` 相等 **或** `threadGuestAccountId === myAccountId`（雙側非空且 trim 後）。
- **積分規則改為**：
  - 建立私線（該線第 1 句）`+5`
  - 未解鎖前（第 2~10 句，按送出前計數 `< 10`）每句 `+2`
  - 第 11 句起不再每句加分
- **前端同步**（`useMessagingActions.ts`）：
  - 聊聊與膠囊彈窗發送都依「送出前訊息數」顯示對應 toast（`建立私線` / `私線回覆（未解鎖前）`）。

## 他人空間看不到已發內容：補上「空間公開」操作

- **原因**：送膠囊時 `isProfilePublic` 先前被固定傳 `false`，且投稿頁沒有對應開關，使用者無法決定是否在個人空間公開。
- **調整**：
  - `SpacetimeMailboxApp.tsx`：`sendCapsuleMessage` 改由表單欄位 `isProfilePublic` 決定，不再硬編碼。
  - `ComposeMessageMainSection.tsx`：膠囊模式新增「同步顯示到我的空間」切換開關（checkbox `name="isProfilePublic"`），並附說明文案。

## 空間內容卡片：可直接開關展示/隱藏

- **新增後端 reducer**：`set_capsule_profile_visibility({ capsuleId, isProfilePublic })`
  - 僅膠囊作者可切換
  - 更新 `capsule_message_space_state.isProfilePublic`
- **前端空間卡片操作**（`SpaceMainSection.tsx`）：
  - 自己的膠囊卡新增按鈕：
    - `空間展示中（點擊隱藏）`
    - `空間已隱藏（點擊展示）`
  - 仍保留既有「刪除膠囊」按鈕
- **接線**（`SpacetimeMailboxApp.tsx`）：
  - 接入新 reducer 並將 `isCapsulePublicInSpace` 傳入空間主區
  - 切換成功顯示 toast，失敗寫入 `squareActionError`
- **綁定更新**：
  - 已執行 `npm run spacetime:generate`，生成 `set_capsule_profile_visibility_reducer.ts`

## 空間展示開關無反應 + 刪除改自定義確認

- **開關無反應原因**：`set_capsule_profile_visibility` 先前僅允許 `authorIdentity === sender`；換設備重登後 identity 變更會被拒。
- **後端修正**：
  - 作者判斷改為三重容錯：`authorIdentity` 或 `authorAccountId`（trim）或 `authorEmail`（normalize）任一匹配即可切換。
- **刪除交互**：
  - `SpaceMainSection` 移除 `window.confirm`，改為頁內自定義確認彈窗（取消 / 確認刪除）。

## 空間側欄文案與資料顯示修正

- **問題**：自己的空間仍顯示「朋友動態」；查看他人空間時缺少對方資訊欄。
- **調整**：
  - `SpaceSidebarSection`：
    - 自己空間副標改為「我的空間」
    - 他人空間副標改為「對方資訊」
    - 新增資訊區（性別 / 稱號 / 說說），無資料時顯示「未設定 / 未知 / 尚無說說」
  - `SpacetimeMailboxApp`：傳入 `profileGender`、`profileNote`、`titleLabel`（自己用本地稱號；他人先以可得資料顯示，缺值保留未知）

## 對方資訊展示規則收斂（性別圖示 / 僅自己顯示積分）

- **需求**：不論空間頭部、左側或查看個人資訊，對方性別只顯示圖示；對方不顯示積分，積分只顯示自己。
- **調整**：
  - `SpaceSidebarSection.tsx`：對方隱藏積分 chip，改為 `GenderIcon` 圖示；自己保留積分 chip。
  - `SpaceMainSection.tsx`：空間頭部同規則，對方不顯示積分且用 `GenderIcon`。
  - `OverlayModalsSection.tsx`：聊天「對方資訊」彈窗移除「男/女」文字，改用 `GenderIcon` + 年齡。
  - `SpacetimeMailboxApp.tsx`：對方空間傳參時 `availablePoints` 改為 `undefined`，前端避免誤渲染對方積分。

## 聊聊內 `ys-chat-header` 加頭像（未解鎖用指定圖示）

- `ChatMainSection.tsx`：
  - 頭部左側新增頭像槽位。
  - 未解鎖資訊時，固定顯示 `src/assets/icons/feature/secretfe1.png`。
  - 已解鎖時顯示對方實際頭像。
- `SpacetimeMailboxApp.tsx`：
  - 依 `selectedChatThread` 的對方 `accountId/identity` 回查 `avatarKey`，映射至頭像 URL 後傳入 `ChatMainSection`。

## 聊聊列表：未解鎖前套用預設頭像

- `ChatThreadsSidebarSection.tsx`：
  - 每條聊聊卡頭像顯示規則調整為：
    - 未解鎖（<10 則）固定顯示 `src/assets/icons/feature/secretfe1.png`
    - 已解鎖才顯示對方實際頭像
  - 保留兩行排版（第一行名稱，第二行最新訊息預覽）
