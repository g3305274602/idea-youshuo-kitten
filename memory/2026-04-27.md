# 2026-04-27

## 單活登入與會話失效保護

- 新增 `src/features/app/sessionGuard.ts`，統一判斷會話失效錯誤（如 `尚未登入`、`CONFLICT_IDENTITY`）並提供清理本地登入狀態與重導登入能力。
- 在 `src/features/app/hooks/useMessagingActions.ts` 針對聊聊、膠囊私訊、貼牆、留言、反應等寫入操作補上會話失效攔截；若判定失效，立即清 token 並回登入，避免「看起來在線但全部送不出」。
- 在 `src/features/app/SpacetimeMailboxApp.tsx` 的主發送流程 `sendMessage` 補上同樣的會話失效攔截，保證直送與膠囊送出路徑一致。
- 在 `src/features/app/hooks/useFavoriteHandlers.ts` 補上會話失效攔截，收藏/取消收藏也會遵守同一套會話安全策略。
- 在 `src/features/app/hooks/useAccountFlowHandlers.ts` 將登出流程改為共用 `sessionGuard` 的清理函式，避免各處清理行為不一致。
- 在 `SpacetimeMailboxApp` 開機檢查（有 token 但無 `myProfile`）超時分支加上本地會話清理，減少卡在錯誤 token 的假登入狀態。

## 驗證

- 已執行 `npm run build`，編譯通過。

## 跨設備聊聊紅點修正

- 調整 `src/features/app/chatReadCursors.ts` 的 `computeThreadUnread`：新增 `myAccountId` 判斷，若最後一則訊息為同帳號（`authorAccountId === myAccountId`）則不計為未讀。
- 更新 `src/features/app/SpacetimeMailboxApp.tsx` 呼叫端，將 `myAccountId` 傳入未讀計算，讓跨設備自己發出的訊息不再觸發聊聊紅點。

## 膠囊「看上一條」後前進修正

- 新增 `CAPSULE_FORWARD_SHOWN_KEY`（`src/features/app/constants.ts`），用於儲存「看上一條前」的當前膠囊，作為一次性前進恢復目標。
- 在 `src/features/app/hooks/useMailboxInteractionHandlers.ts` 補上 forward 狀態讀寫，並在 `openCapsuleDrawer`、`closeCapsuleDrawer`、換到空狀態時同步清除，避免殘留。
- 調整 `viewPreviousCapsule`：按下「看上一條」時，先記住當前膠囊為 forward，再切回上一條，維持「只能回看一步」。
- 調整 `pickAnotherCapsule`：若存在 forward 恢復目標，下一次「換一個」優先回到該目標一次；消耗後清空 forward，後續恢復正常隨機流程，不會在兩條之間來回卡住。

## 手機端「發佈中」卡住保護

- 在 `src/features/app/SpacetimeMailboxApp.tsx` 新增 `callWithTimeout`，對 `sendDirectMessage` 與 `sendCapsuleMessage` 套用 12 秒超時保護。
- 若 reducer 呼叫在弱網或連線異常下無回應，UI 不再無限停在「處理中...」，會顯示「送出逾時，請檢查網路或重新登入後再試」並解除 loading。

## 連線首頁超時與前景恢復

- 調整 `src/App.tsx`：新增 10 秒連線超時判斷。若 `isActive/identity` 長時間未就緒，顯示「連線逾時，請重試」而非無限 loading。
- 新增兩個操作按鈕：`重試`（重新整理頁面重連）、`重新登入`（清除 `SPACETIME_TOKEN_KEY` 後重整）。
- 新增前景恢復監聽：當 `visibilitychange` 回到 `visible` 時，自動重置超時計時並觸發一次重試流程觀察，降低手機背景切回後卡住機率。

## 連線重試體感優化（先軟重試）

- 調整 `src/App.tsx` 的 `重試` 行為為「軟重試優先」：先重置連線等待並觸發重試 tick，不立即 reload。
- 新增 `softRetryCount`：連續軟重試失敗達 3 次才自動 `window.location.reload()`，兼顧手機體感與恢復能力。
- 連線逾時卡片補上提示文案，明確告知「連續失敗 3 次會自動重整」。

## 連線頁視覺與文案輪播

- 在 `src/App.tsx` 引入 `src/assets/images/common/cat1.png`，將小貓裝飾圖固定於畫面底部貼底顯示。
- 新增連線文案輪播 `CONNECTING_COPY`：於前 10 秒等待期內每 1.8 秒切換提示文案，首次載入與重試流程都會重置並重新輪播。
- 逾時時改為失敗文案（連線失敗／網路或會話失效提示），並保留 `重試`、`重新登入` 按鈕作為下一步操作。

## 聊聊名稱回復修正

- 調整 `src/features/app/sections/ChatMainSection.tsx` 的標題名稱來源：解鎖後優先使用 `selectedChatPeerProfile.displayName`，若暫無才回退 `counterpartLabel`。
- 修正「先顯示神秘旅人後即使資料同步也不回復真名」的體感問題，避免名稱被 fallback 長期卡住。

## 登入衝突後身份漂移的名稱回補

- 調整 `src/features/app/types.ts`：`CapsuleChatThreadSummary` 新增 `counterpartAccountId`，作為 identity 失配時的穩定回補鍵。
- 調整 `src/features/app/SpacetimeMailboxApp.tsx`：
  - 新增 `profileByAccountId` 映射。
  - 建立聊聊線摘要時，若 `threadGuestIdentity` 查不到 profile，會嘗試從同線訊息的 `authorAccountId` 回補對方資料。
  - `selectedChatPeerProfile` 增加 accountId fallback，讓名稱、對方資訊、TA 空間按鈕在 identity 遷移後仍可恢復。

## 性別選項與中文顯示統一

- `src/features/app/sections/AgeGateModalSection.tsx`：註冊後完善資料的性別改為僅 `男 / 女`（移除其他）。
- `src/features/app/sections/ProfileModalSection.tsx`：編輯資料的性別下拉改為僅 `男 / 女`。
- `src/features/app/hooks/useAccountFlowHandlers.ts`：新增 `normalizeBinaryGender`，所有提交路徑（註冊、完善資料、編輯資料、自介更新）都強制正規化為 `male / female`。
- `src/features/app/sections/OverlayModalsSection.tsx`：性別顯示改為中文映射（男/女/未填），不再直接顯示英文值。
- `src/features/app/appUiStore.ts`：`registerGender` 與 `ageGateGender` 預設值調整為 `male`，避免初始落在不允許選項。

## 年齡年份範圍與預設定位調整

- `src/features/app/SpacetimeMailboxApp.tsx`：年份選項改為「當前年份一路回溯到 1900」。
- `src/features/app/hooks/useAccountFlowHandlers.ts`：當使用者尚無生日資料時，預設生日改為「剛好最小可選年齡（今日 - 16 年）」而非固定舊年份，避免初始停在 1900 或其他非預期年份。

## 全局按鈕去邊框

- `src/index.css`：於 `@layer base` 新增全域按鈕規則，對 `button` 與 `:hover/:focus/:active` 統一 `border: none !important;`，避免全站出現按鈕外框線。

## 手機膠囊 FAB 垂直位置微調

- `src/index.css`：將 `.ys-mobile-bottom-nav-fabSlot .ys-mobile-capsule-button` 由 `top-0` 調整為 `top-1`，使膠囊按鈕整體下移一點，避免視覺過於貼上。

## 收藏頁空狀態垂直置中

- `src/features/app/sections/FavoritesSidebarSection.tsx`：空狀態容器改為 `flex` 垂直置中（`h-full + min-h-[46vh] + items-center + justify-center`），讓提示區塊在手機頁面視覺上更接近中間位置。

## 多頁空狀態垂直置中統一

- `src/features/app/SpacetimeMailboxApp.tsx`：信箱列表空態「暫無信件」改為垂直置中（`min-h-[46vh]`）。
- `src/features/app/sections/ChatThreadsSidebarSection.tsx`：聊聊側欄空態改為垂直置中。
- `src/features/app/sections/MyReportsSidebarSection.tsx`：我的舉報空態改為垂直置中。
- `src/features/app/sections/ChatMainSection.tsx`：未選聊聊線時的空態改為垂直置中。
- `src/features/app/sections/ComposeMessageMainSection.tsx`：訊息詳情空態（選一則訊息）改為垂直置中。

## 頂欄固定與背景跟隨主背景

- `src/features/app/sections/TopNavSection.tsx`：頂欄改為 `fixed top-0`，並移除自身底色／模糊（`bg-transparent` + `backdrop-blur-none`），讓視覺直接吃主背景。
- `src/features/app/SpacetimeMailboxApp.tsx`：在頂欄下方補 `56px` 高度 spacer，避免內容被固定頂欄覆蓋。

## 右上帳號鈕改為僅桌面顯示

- `src/features/app/sections/TopNavSection.tsx`：右上帳號切換按鈕（原本 `md:hidden` 的行動版鈕）調整為 `hidden md:inline-flex`，僅在電腦端顯示，手機端隱藏。

## 頂欄重複帳號鈕修正

- `src/features/app/sections/TopNavSection.tsx`：將「行動版帳號鈕」改為 `className="hidden"`，避免與既有桌面下拉帳號鈕重複顯示；桌面只保留原本右側下拉那一顆。

## 秘密頁跳廣場牆的返回路徑修正

- `src/features/app/hooks/useNavigationActions.ts`：
  - 從 `secret` 跳到 `mine_square` 時，記錄返回目標為 `secret`。
  - 在手機返回邏輯中，若目前在 `mine_square` 列表層且有返回目標，優先返回 `secret`，避免被默認導回「我的」。
- 補修一處覆蓋問題：`onMineHubNavigate` 末尾原本會無條件清掉 `chatBackTab`，現改為「從 secret 跳 mine_square 時保留」，確保返回能真的回到來源頁。

## 編譯錯誤修正（SquarePost 欄位名）

- `src/features/app/SpacetimeMailboxApp.tsx`：將誤用的 `snapshotPublisherAccountId` 改為正確欄位 `publisherAccountId`（2 處），修復 `TS2551` 編譯錯誤。

## 抽膠囊排除自己（含 identity 漂移）

- `src/features/app/SpacetimeMailboxApp.tsx`：抽膠囊候選與空態判定由「僅比對 `authorIdentity`」改為「`authorIdentity` + `authorAccountId` 雙重比對」，避免登入衝突或 identity 遷移後仍抽到自己發的膠囊。
