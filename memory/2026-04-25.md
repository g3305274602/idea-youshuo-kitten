# 2026-04-25

## SpacetimeMailboxApp 第一階段拆分

- 抽離導航判斷邏輯到 `src/features/app/shell/navigation.ts`。
- `SpacetimeMailboxApp` 改為使用共用導航函式（`isAdminTab`、`computeIsMobileDetailView`、`isMineSubTab`、`canShowMobileBackButton`）。
- 新增巢狀彈窗基礎 hook `src/features/app/shell/modalStack.ts`（先建立能力，暫不接管既有彈窗流程）。
- 本階段未更動 reducer 呼叫、DB 訂閱條件與功能流程。

## SpacetimeMailboxApp 第二階段（部分）拆分

- `modalStack` 擴充 `dismissLastByType` 與 `isOpen`，提供巢狀彈窗最小操作集。
- `SpacetimeMailboxApp` 先接入 `profile/password` 彈窗到 `modalStack`，採相容模式（保留原本布林狀態 + stack 同步）。
- `profile/password` 開啟與關閉路徑改為同步更新 stack，既有提交邏輯與 reducer 呼叫時機不變。
- 新增 `report` 與 `outboxDeleteConfirm` 的 stack 同步開關（遮罩關閉、取消按鈕、提交成功後關閉皆同步更新）。
- 新增 `chatPeerProfile` 的 stack 同步開關，與既有布林狀態並行，保持功能相容。
- `adminAdd` / `adminEdit` 改為透過 stack 包裝 setter 同步開關，`AdminModals` 接收可見性改為「布林 or stack」相容模式。
- `adminReportModal` 也改為 stack 包裝 setter（`onSetAdminReportModalOpen`）並接入相容可見性。
- `publish`、`introEdit`、`banNotice` 接入 stack 相容可見性與開關同步（含遮罩/取消/提交後關閉路徑）。
- `SpacetimeMailboxApp` modal 型別改為共用 `ModalType`，新增 `syncModalOpen` 以降低後續擴充漏改風險。
- 將既有已接入 stack 的彈窗開關全面改為 `setXxxOpenWithStack` 呼叫，移除分散的 `setOpen + open/closeModalInStack` 重複寫法。
- 抽離 `OverlayModalsSection`（chat peer、outbox delete confirm、ban notice、report）以縮減 `SpacetimeMailboxApp` 主檔 modal 渲染密度。
- 抽離 `AccountModalsSection`（introEdit、password）並改由主檔傳參控制，進一步降低主檔複雜度。
- 抽離 `ProfileModalSection`（含 WheelPicker 依賴），主檔改以 props 注入控制與提交。
- 抽離 `PublishModalSection`（公開範圍、評論開關、隱私顯示與提交按鈕）並改為主檔注入狀態與事件。
- 抽離 `MobileBottomNavSection`，將手機底部導航改為獨立可替換區塊，便於改版時單點替換樣式與交互。
- 抽離 `ProfileActionMenuSection`，將「我的」頁編輯選單彈窗集中管理，便於改版時重做選單視覺與操作排序。
- 抽離 `AgeGateModalSection`（性別 + 生日滾筒 + 提交），將完善資料流程從主檔遷出，便於後續改版。
- 抽離 `TopNavSection`（返回邏輯入口、桌面分頁入口、個人下拉入口），主檔改為傳遞事件與狀態，便於重做 Header UI。
- 抽離 `StatusBannersSection`（未填資料提醒 + 警告/禁言跑馬燈），主檔僅保留狀態組裝與訊息字串，便於後續獨立重做上方提示視覺。
- 抽離 `SpaceMainSection`（空間主內容列表與動作列），主檔改以 props 注入資料與事件，保留刪除膠囊／撤下廣場／發起聊聊／查看原貼既有流程不變。
- 建立 `src/features/admin/AdminWorkbench.tsx` 作為管理員功能入口層，`SpacetimeMailboxApp` 的管理員三塊渲染改由 `AdminSidebarWorkbench`、`AdminContentWorkbench`、`AdminModalsWorkbench` 轉接，為下一步將 admin state/reducer/useTable 從主檔遷出做準備。
- 新增 `src/features/admin/useAdminWorkbench.ts`，將管理員本地狀態（報告篩選、處分草稿、授權編輯、行動狀態）與主要操作 handler（建立首位超管、認領失聯超管、舉報處理、處分套用、管理員授權、一鍵停權/復權）從 `SpacetimeMailboxApp` 抽離為 admin feature hook。
- 在 `useAdminWorkbench.ts` 增加 `useAdminWorkbenchTables`，將管理員相關資料訂閱（adminRole/adminProfiles/adminAudit/report/sanction 等）遷出主檔；`SpacetimeMailboxApp` 改為透過 hook 取得管理資料，進一步降低主檔對管理員資料層耦合。
- 在 `useAdminWorkbench.ts` 增加 `useAdminWorkbenchDerived`，把管理員衍生計算（報表排序、選中舉報/快照、孤兒超管可認領判定、帳號搜尋、目標帳號生效處分、授權候選與角色標籤）從主檔遷出，`SpacetimeMailboxApp` 只保留調用與傳遞。
- 在 `useAdminWorkbench.ts` 增加 `useAdminWorkbenchRuntime` 聚合入口（tables + derived + actions），`SpacetimeMailboxApp` 改為單次調用取得管理員資料與操作，進一步收斂主檔對 admin 內部組件的認知面。
- `SpacetimeMailboxApp` 移除分散式 admin hooks 調用與對應殘餘匯入，改為以 `useAdminWorkbenchRuntime` + `useAdminWorkbenchState` 雙層接入管理員功能；保留既有功能行為與資料載入門檻不變。
- 將管理員內容區中的刪除操作 handler（刪除膠囊 / 刪除廣場貼文 / 刪除管理角色列）抽離到 `useAdminWorkbenchActions`，`SpacetimeMailboxApp` 改用 runtime 暴露的 action，減少主檔內 inline async 邏輯。
- 將 `AdminSidebarWorkbench`、`AdminContentWorkbench`、`AdminModalsWorkbench` 的長 props 清單集中為 `adminViewProps`（透過 `buildAdminViewProps` 打包），主檔改為 spread 掛載三塊管理員 UI，降低主檔渲染區可視複雜度並為下一步完整入口容器化鋪路。
- 清理 `SpacetimeMailboxApp` 中 runtime 解構後未使用的管理欄位（`rawIsAdmin`、`adminProfiles`、`adminAuditLogRows`），維持主檔最小依賴面並保持編譯通過。
- 新增 `createAdminViewProps`（`src/features/admin/AdminWorkbench.tsx`），將 `adminViewProps` 的組裝邏輯從 `SpacetimeMailboxApp` 搬到 admin feature；主檔改為單一函式呼叫產生三塊 props，進一步減少主檔內管理員區塊雜訊。
- 新增 `AdminWorkbench` 單一入口元件（slot: `sidebar` / `content` / `modals`），`SpacetimeMailboxApp` 改為使用同一個入口元件掛載管理員三區塊，完成主檔對 admin 呈現層的統一接入。
- 新增 `zustand` 依賴，建立 `src/features/admin/adminWorkbenchStore.ts`，並將 `useAdminWorkbenchState` 改為讀取 Zustand store（取代原本一整段 `useState`），完成 admin 本地狀態向 store 轉換且保持行為相容。
- 新增 `src/features/app/appUiStore.ts`，先將 `SpacetimeMailboxApp` 的核心跨區 UI 狀態（view/activeTab、選中訊息、space 跳轉來源、主要 modal 開關）收斂進 Zustand；`SpacetimeMailboxApp` 改為透過 `useAppUiStore` 讀寫，保留既有 reducer 呼叫與功能流程不變，並通過 build 驗證。
- 擴充 `src/features/app/appUiStore.ts` 承接 `SpacetimeMailboxApp` 全部剩餘本地狀態（compose/profile/auth/report/outbox/birth/mailbox/即時 UI flags 等），`SpacetimeMailboxApp` 內 `useState` 全數移除，改由 `useAppUiStore` 單一來源管理；build 與 lints 皆通過。
- 新增 `src/features/app/sections/PickerControls.tsx`，將 `WheelPicker` 與 `GenderIcon` 從 `SpacetimeMailboxApp` 抽離為可重用呈現元件；主檔改為匯入使用，功能與樣式保持一致並通過 build/lints。
- 新增 `src/features/app/hooks/useAccountFlowHandlers.ts`，將登入/登出、個資編輯、密碼修改、年齡閘與舉報流程 handler 從 `SpacetimeMailboxApp` 抽離；主檔改為注入參數後取得 actions，行為不變並通過 build/lints。
- 新增 `src/features/app/hooks/useMailboxInteractionHandlers.ts`，抽離膠囊抽取/切換/關閉、由膠囊跳聊聊、開啟空間、刪除膠囊、已寄出編輯（載入/取消/儲存/刪除）等互動流程；主檔改為注入依賴並使用回傳 actions，行為維持一致。
- 新增 `src/features/app/hooks/useFavoriteHandlers.ts`，抽離廣場與膠囊的收藏/取消收藏與錯誤處理；主檔只保留調用。
- 本輪拆分後 `SpacetimeMailboxApp.tsx` 行數降至 4483，並通過 build / lints。
- 新增 `src/features/app/hooks/useMessagingActions.ts`，抽離訊息/廣場互動流程（貼牆、撤牆、反應、往來摘錄、留言、膠囊私訊、聊聊送出與輸入框自適應）；主檔改為統一調用 hook actions。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數進一步降至 4364，build 與 lints 全部通過。
- 新增 `src/features/app/hooks/useNavigationActions.ts`，抽離頂部導航與我的頁入口的高密度 inline callback（admin mode back、mobile back、secret/compose/mine 切頁、admin 入口、MineHub 導航），主檔改為傳入 actions。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 4311，並通過 build / lints。
- 新增 `src/features/app/hooks/useAsideActions.ts`，抽離 Aside 區塊返回流程與「剛開啟播報」互動 callback（返回我的、按 scope 收起、清空、鍵盤打開、單則關閉、空間側欄返回）；主檔 JSX 進一步去除 inline 邏輯。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 4295，並通過 build / lints。
- 新增 `src/features/app/sections/SpaceSidebarSection.tsx`，抽離空間側欄返回按鈕與旅人統計卡片，主檔改為 props 掛載。
- 新增 `src/features/app/sections/MyReportsSidebarSection.tsx`，抽離我的舉報側欄清單（過濾、排序、狀態展示），主檔移除對應 IIFE 內嵌渲染。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 4168，並通過 build / lints。
- 新增 `src/features/app/sections/ChatThreadsSidebarSection.tsx` 與 `src/features/app/sections/FavoritesSidebarSection.tsx`，抽離 Aside 的聊聊清單與收藏清單渲染。
- 新增 `src/features/app/sections/MineSidebarSection.tsx`，抽離「我的」側欄個人卡片與入口大卡渲染，主檔只保留資料組裝與事件注入。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 4042，並通過 build / lints。
- 新增 `src/features/app/sections/ChatMainSection.tsx`，抽離主內容區的聊聊詳情面板（標題、解鎖、訊息流、輸入送出）。
- 新增 `src/features/app/sections/SecretMainSection.tsx`，抽離主內容區的秘密分頁（未選中/已選中貼文）大段渲染與互動按鈕。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 3632，並通過 build / lints。
- 新增 `src/features/app/sections/FavoritesMainSection.tsx`，將 `activeTab === "favorites"` 的主內容詳情卡與互動按鈕完整抽離，主檔改為單一 section 掛載並透過 action props 注入行為。
- 本輪完成後已再次執行 `npm run build` 與針對變更檔案的 lints 檢查，均通過；功能流程（收藏移除、膠囊發起聊聊、回跳秘密貼文）保持不變。
- 新增 `src/features/app/sections/ComposeMessageMainSection.tsx`，將主內容中 `compose(new/direct)` 與 `message detail(inbox/outbox)` 的整段 AnimatePresence 渲染抽離，主檔改為資料/事件注入，保留發送、貼牆、撤牆、往來摘錄、編輯刪除入口等既有流程。
- 新增 `src/features/app/sections/CapsuleOverlaySection.tsx`，將膠囊抽取的大型 overlay（標頭、內文、反應/收藏、私線、評論、底部操作）完整抽離，主檔保留狀態組裝與 action 傳遞。
- 本輪完成後 `SpacetimeMailboxApp.tsx` 行數降至 2795，並通過 `npm run build` 與 lints 檢查。
- 收尾整理：`SpacetimeMailboxApp.tsx` 移除已無使用的 import（`AnimatePresence`、`motion/react-client` 的 `div`），降低主檔雜訊。
- 收尾整理：新增 `composeMessageMainProps` / `capsuleOverlayProps` 兩個集中 props 物件，改為 spread 傳入 `ComposeMessageMainSection` 與 `CapsuleOverlaySection`，主檔維持邏輯不變但可讀性更高。
- 收尾整理完成後再次通過 `npm run build` 與 lints；目前 `SpacetimeMailboxApp.tsx` 行數為 2796（僅因整理新增 props 集中區，功能不變）。
- 清理 `src/features/app/SpacetimeMailboxApp.tsx` 內未使用項：移除多個未再引用的 icon imports、`CAPSULE_LAST_SHOWN_KEY` / `CAPSULE_TYPE_OPTIONS`、`MetaItem` / `MobileNavItem` / `NavTab`、`GenderIcon`、`Countdown` / `isoToDatetimeLocalValue` / `squareAddressSubtitle` 等殘留匯入。
- 清理後已重新 `npm run build` 通過；IDE lints 顯示一筆舊診斷（`X` 未定義）但與最新編譯結果不一致，屬快取延遲。
- 第二輪清理：將 `SpacetimeMailboxApp.tsx`、`sections/AdminSection.tsx`、`sections/AuthSection.tsx`、`sections/PickerControls.tsx` 的 `React` 預設匯入改為 `import type React`（保留 hooks 的 named import），移除僅型別用途的 runtime 匯入殘留。
- 第二輪清理後已再次通過 `npm run build` 與 `src/features/app` 範圍 lints。
- 第三輪清理（含擴展位）：`SecretMainSection`、`ChatMainSection`、`ComposeMessageMainSection`、`CapsuleOverlaySection` 的 props 補上 `extension?: Record<string, unknown>`，保留後續擴充入口而不影響現有行為。
- 第三輪清理（型別收斂）：移除 `src/features/app` 內殘留 `as any` 與空 `onChange` noop；`SecretMainSection` 改為明確 `SquarePost` / `SquareComment` / reaction map 型別，`ChatMainSection` 補齊聊天訊息與 ref 型別，`SpacetimeMailboxApp` 對應改為 `null` 安全傳參與直接傳遞唯讀 map/set。
- 第三輪完成後 `rg "as any|onChange={() => {}}" src/features/app` 已無匹配，並通過 `npm run build`。
- 頁面整理：在 `SpacetimeMailboxApp.tsx` 新增並統一使用 `topNavProps`、`mobileBottomNavProps`、`chatMainProps`、`secretMainProps`、`overlayModalsProps`、`publishModalProps` 等集中 props 物件，將多段超長 inline props 改為 spread 傳遞，提升主檔可讀性與後續擴展可維護性（邏輯不變）。
- 本輪頁面整理後再次通過 `npm run build` 與 `SpacetimeMailboxApp.tsx` lints。
- UI 改版第一批：更新 `sections/TopNavSection.tsx` 與 `sections/MineSidebarSection.tsx` 的視覺層次（導航背景、按鈕圓角與邊框、個人下拉卡片質感、Mine 概覽資訊卡與統計 chips），不變更事件與資料流。
- UI 第一批改版後通過 `npm run build` 與對應檔案 lints。
- 依使用者指示移除 `MineSidebarSection` 的「Mine Dashboard」額外統計卡，回復為個人卡 + 功能卡主體，不再主動加入額外區塊。
- UI 改版第二批：更新 `sections/SecretMainSection.tsx` 與 `sections/ComposeMessageMainSection.tsx`，對齊提供的深色星空/玻璃質感方向（背景漸層、卡片玻璃感、按鈕與輸入框配色層次），保留原邏輯與事件流不變。
- UI 第二批改版後通過 `npm run build` 與對應檔案 lints。
- UI 改版第三批：更新 `sections/ChatMainSection.tsx` 與 `sections/CapsuleOverlaySection.tsx`，整體由粗邊框/大陰影改為細邊框、緊湊間距與夜色玻璃卡片，對齊「元素邊距不要太大、不要粗獷」要求；互動事件與資料流不變。
- UI 第三批改版後通過 `npm run build` 與對應檔案 lints。
- UI 樣式系統重整：在 `src/index.css` 新增語意化可重用 class（`ys-night-*`、`ys-btn-*`、`ys-chat-*`、`ys-capsule-*`），將夜色玻璃主題抽成共用樣式層，降低 section 內長串 utility class。
- 套用語意化樣式：`sections/ChatMainSection.tsx` 與 `sections/CapsuleOverlaySection.tsx` 改為優先使用語意 class（面板、氣泡、輸入、主按鈕、彈窗容器等），維持既有邏輯/事件流，後續可在 CSS 單點微調整體視覺。
- 我的頁面重做：`sections/MineSidebarSection.tsx` 改為按參考圖重建單頁結構（星空背景、個人資訊卡、四個統計、白底功能清單），未提供素材的頭像位置先保留空白框；圖示統一採 SVG（lucide-react）。
- 新增 `index.css` 的 `ys-mine-*` 語意化樣式群組，將「我的」頁面視覺集中到 CSS 層管理，保留既有 `onNavigate`/設定開啟/編輯簡介等事件流不變。
- 底部導航重做：`sections/MobileBottomNavSection.tsx` 改為「左：撰寫｜中：膠囊抽取｜右：我的」排列；中間按鈕改為浮起黃金膠囊主按鈕，並保留原事件路由（`onGoCompose`、`onGoSecret`、`onGoMine`）不變。
- 新增 `index.css` 的 `ys-mobile-*` 樣式群組，集中管理底部導航與中間膠囊按鈕視覺；建置與 lints 皆通過。
- 底部弧度與圖示精修：`ys-mobile-bottom-nav` 增加上緣圓角（28px）、浮起陰影與玻璃高光，貼近參考圖的弧形底欄觀感；中間膠囊圖示改為自繪雙色 SVG（含高光與分色），取代先前簡單線框 icon。
- 底欄定位修正：移除 `ys-mobile-bottom-nav` 的外邊距與外浮陰影，改為貼底容器（僅保留上緣弧度與內高光）；中間膠囊按鈕上浮量由 `-mt-8` 微調為 `-mt-7`，避免整塊視覺「漂出容器」。
- 色票校正：依最新參考圖重新調整 `ys-mine-*` 與 `ys-mobile-*` 色系（我的頁背景、個人卡、頭像框、白底清單、底部導航與金色膠囊按鈕），將偏綠色調修正為藍青深底 + 灰白清單 + 暖金主按鈕的組合。
- 背景層級調整：將「我的」頁主題色從 `MineSidebarSection` 區塊上提到整個 App 層（`ys-app-mine-theme`），並同步到 `TopNavSection` 與 aside 容器，讓頁面空白區與頭部可保持同色同紋理連續背景。
- 結構收斂：`MineSidebarSection` 移除獨立背景外框與星點覆蓋，改為僅承載內容排版，避免出現「只包一段」的割裂色塊。
