---
description: 「Cosmic Drift」品牌與 UI 設計規範（全專案預設、可依使用者指示迭代）
alwaysApply: true
---

# Cosmic Drift 設計方案

**全專案 UI/前端預設遵守本規則**（與舊有「夢幻星空」等描述並存時，**以本規則為準**）。實作 CSS 變數時可對齊 `src/index.css` 的 token 命名。

## 如何維持一致與修正
- 新功能、重構、修樣式時，預設對齊本文件的色票、字體、圓角與頁面定義。
- 使用者**隨時可要求**微調規格文字或程式實作；收到指示後，應**同步更新**本 `.mdc`（若屬持久規則變更）與相關 `tsx/css`，做到規範與畫面一致，而非只做單次硬改。

## 品牌理念
- **關鍵字**：宇宙、神祕、溫暖、連結、膠囊。
- **氛圍**：深邃太空背景 + 高飽和星雲色；高度圓潤語言，象徵膠囊般的包容。
- **模式**：暗色、高對比、柔和圓角。

## 色票
| 用途 | 色值 | 說明 |
|------|------|------|
| Primary | `#FFD54F` 星光黃 | 主按鈕、核心圖示（如金膠囊）、希望與亮點 |
| Secondary | `#F06292` 星雲粉 | 分類標籤、發送鈕等柔和強調 |
| 主背景 | `#121319` 深空黑 | 頁面底 |
| 容器/卡片 | `#1A1B22` 深灰藍 | 與主背景區隔 |
| 標題/內文 | `#FFFFFF` | 主要文字 |
| 輔助 | `#8E8E93` 等灰階 | 說明、次級資訊 |

## 字體
- **家族**：Plus Jakarta Sans，後備系統 sans-serif。
- **階層**：大標 24px Bold；次標 18px Semibold；本文 14px Regular；輔助 12px Light。

## 圓角與組件
- **圓角**：多用 **膠囊全圓** `border-radius: 999px`，或大塊內容 **24px** 超大圓角。
- **主按鈕**：膠囊形、Primary 或 Secondary 底；文字用反白或與黃/粉對比足夠的深字。
- **次按鈕**：幽靈按鈕 + 細邊框（如 `border-white/10` 或品牌色相關的淡邊）。
- **卡片**：微邊框或比主背景再深一階的底；圓角與全站一致。

## 插畫與符號
核心視覺：穿太空服黑貓、行星、**發光膠囊**；與主色金膠囊呼應。

## 核心頁面（實作導向）
1. **首頁/抽膠囊**：畫面中心金膠囊，點擊為主要互動入口。
2. **撰寫膠囊**：標籤式分類（如 #心情、#職場）+ 簡潔文字輸入。
3. **廣場牆**：卡片流、匿名分享；支援讚與留言。
4. **聊天**：氣泡左右分 sender；配色沿用 Primary/Secondary 與背景層次。
5. **個人空間**：Profile 與功能清單導覽清楚分區。

## AI 實作提示
- 新畫面優先使用上表色值與圓角規範，避免自創一套衝突配色。
- 可抽成 CSS 變數如 `--cd-primary`、`--cd-bg`、`--cd-surface`，便於主題一致。
- **`src/index.css` 內建 Cosmic 工具類**（彈窗／表單優先複用，勿回退郵票粗邊＋米白底）：`cd-modal-panel`、`cd-field`、`cd-btn-ghost`、`cd-btn-primary`、`cd-btn-danger`、`cd-card-raised`。
